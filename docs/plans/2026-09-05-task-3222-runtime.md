# TASK-3222 -- sm-ui portal runtime (createAppGate, createApiProxy) and the sections/permkeys bins

## Identity

- Waffle item: TASK-3222 (`bug_ad32897632a71f6e`), status `open`.
- Allowed repository: `sprint-mode/sm-ui`.
- Item binding: `sha256:07d430220618ca7bb273357f904fdaf745534ce2ce45fd9b415f81e4d8b22085`.
- Waffle source revision: `updated_at` `2026-09-06 00:26:03`; the canonical contract is stored on TASK-3222.
- Planning base: `origin/main` at `3513500`.
- Feature branch: `task-3199-runtime` (already pushed, PR #374, head `44abe8548b34b19fa010aa0398facf6c10c80688`); target branch: `main`.
- Design binding: none. FEAT-3170 factory sequence step 1a, proposal bc_cf23c2095a71a229 section 1, Aaron GO D1 and D3 in bc_c3e28774e7b8333e.
- Decision owner: Aaron Hall (`ct_d47be523b3efbea5`). Operating mode: rails.
- Waffle is authoritative. This plan documents the already-implemented sm-ui scope for landing.

## Readiness keys

| key | how it is satisfied |
|---|---|
| `item_binding` | Computed from TASK-3222's stored sections and revision via `wf-ready.py`'s `item_binding`. |
| `plan_binding` | This committed file. |
| `repository` | `sprint-mode/sm-ui`, matching the item's Allowed repository. |
| `feature_branch` | `task-3199-runtime`, PR #374, bound to TASK-3222 via the PR body's Item line. |
| `target_branch` | `main`, the single entry in `landing_branches`. |
| `base_sha` | `origin/main` at `3513500` at planning time; re-read at verification time. |
| `working_tree` | Clean; the runtime and bins were already committed at branch head. |
| `git_metadata` | A fresh clone under the session scratchpad, not a linked worktree. |
| `github_identity` | `sprint-mode-automation[bot]` via the sm-gh proxy wrapper. |
| `waffle_identity` | The session's Waffle MCP identity. |
| `push_capability` | Ordinary branch push; no `.github/` path touched by this square. |
| `commands` | `npm ci`, `npm run lint`, `npm run type-check`, `npm run build`, `npm run test`. |
| `dependencies` | None added. |
| `target_ci` | `ci-gate` on the exact PR head, in `sprint-mode/sm-ui`. |

## Tasks

| # | Task | Stored `Done when` slice | Evidence |
|---|---|---|---|
| 1 | `runtime/middleware.js` exporting `createAppGate(options)`. | PR landed through `/merge` with fresh APPROVE and green CI | `src/__tests__` runtime middleware tests. |
| 2 | `runtime/api-proxy.js` exporting `createApiProxy(portal)`. | Same | runtime api-proxy tests. |
| 3 | `runtime/index.js` re-exporting both; `./runtime` package export; shipped in tarball. | Published package version carries `runtime/` | `package.json` diff; `npm view`/release tag. |
| 4 | `bin/sm-portal-sections-sync.mjs` and `bin/sm-portal-permkeys.mjs`, ESM ports of the template scripts, registered in `package.json` bin. | Published package version carries the two bins | `package.json` diff; bin fixture tests. |
| 5 | README consumer usage. | Supports discoverability. | `README.md` diff. |
| 6 | Land the PR through `/merge`: fresh review, green CI, merge queue. | `PR landed through /merge with a fresh APPROVE and green CI` | PR #374 URL; merge SHA. |

## Planned mutation paths

| Repository-relative path | Operation | Pair |
|---|---|---|
| `runtime/middleware.js` | `create` | `-` |
| `runtime/api-proxy.js` | `create` | `-` |
| `runtime/index.js` | `create` | `-` |
| `bin/sm-portal-sections-sync.mjs` | `create` | `-` |
| `bin/sm-portal-permkeys.mjs` | `create` | `-` |
| `package.json` | `modify` | `-` |
| `README.md` | `modify` | `-` |
| `src/__tests__/**` (runtime + bin tests) | `create` | `-` |

## Risks

- **Behavior must stay byte-for-byte equivalent to sm-portal-template main `38b1e09f`.** Already verified by the previous build session; no logic changes made in this landing pass.
- **This PR was originally bound to TASK-3199 (template-repo scope).** TASK-3222 was created to carry the sm-ui-only half of the work; the PR body's Item line was rebound to TASK-3222 (bug_ad32897632a71f6e) before this plan doc was written.

## Execution controls

- This plan documents already-completed implementation for landing purposes only; no new implementation commits are anticipated beyond conflict resolution or review-driven fixes. Retry ceiling: 2 per failing verification command.

The plan ends before landing; `/merge` takes the branch through the queue.
