Follow AGENTS.md at the repository root. It is the workflow contract for this
repo: PR-only landing via the merge queue (/merge), no red PRs, migration and
deploy rules. When in doubt, AGENTS.md wins.
