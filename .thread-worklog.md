CHROME-2 worklog
- 2026-08-19: diagnosed BUG-2276 remaining dock defects + TASK-2282.
  1a: Mode/Hub are status=active (not draft) -> FLAGGED to Aaron, not built.
  1b: staged in Layout.tsx PortalPicker (null logo_mark_url -> SM boxed-chevron default).
  2282: staged in Layout.tsx (removed NotificationBellNav header render + import + notificationBellEnabled var).
  Studios (sm-studios/src/App.tsx): removed Updates from CLIENT_NAV + Cmd-K palette.
  NOT cut a version, NOT opened PRs, NOT run bump wave — holding for 1a to ship one combined sm-ui version + one wave.
  Branches not yet created. sm-ui type-check+lint clean; sm-studios build clean.
